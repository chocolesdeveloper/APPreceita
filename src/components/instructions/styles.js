import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    padding: 8,
    gap: 8,
  },
  textIndex: {
    fontWeight: "bold",
    fontSize: 18,
  },
  textInstruction: {
    flex: 1,
    lineHeight: 20,
  },
})
